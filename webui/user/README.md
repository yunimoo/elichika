# User features for WebUI
- Requires user to login via user id and password (if they have set one)
- The user id and password can be remembered in localStorage to avoid authenticating again, but authenticating is a must.
