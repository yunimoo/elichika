// place holder for unhandled features
package userdata

import (
	"fmt"
)

func (session *Session) AddNaviAction(naviActionID int) {
	fmt.Println("TODO: AddNaviAction, id: ", naviActionID)
}

func (session *Session) AddStorySide(storySideID int) {
	fmt.Println("TODO:  AddStorySide, id: ", storySideID)
}
